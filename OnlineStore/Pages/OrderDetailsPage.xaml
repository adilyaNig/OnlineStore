<Page x:Class="OnlineStore.Pages.OrderDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:OnlineStore.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="OrderDetailsPage">

    <Grid>
        <StackPanel>
            <TextBlock Text="{Binding Order.OrderDate, StringFormat='Order Date: {0:d}'}"/>
            <TextBlock Text="{Binding Order.Status, StringFormat='Status: {0}'}"/>
            <ListView x:Name="lvOrderItems">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Product" DisplayMemberBinding="{Binding Product.Name}"/>
                        <GridViewColumn Header="Price" DisplayMemberBinding="{Binding UnitPrice, StringFormat=C}"/>
                        <GridViewColumn Header="Quantity" DisplayMemberBinding="{Binding Quantity}"/>
                    </GridView>
                </ListView.View>
            </ListView>
            <Button Content="Back" Click="btnBack_Click" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Page>
