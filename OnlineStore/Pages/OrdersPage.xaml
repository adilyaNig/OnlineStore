<Page x:Class="OnlineStore.Pages.OrdersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:OnlineStore.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="OrdersPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Кнопки управления -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <Button Content="Назад" Click="btnBack_Click" Margin="5" Width="100"/>
            <Button x:Name="btnCreate" Content="Создать заказ" Click="btnCreate_Click" 
                    Width="120" Margin="5"/>
            <Button x:Name="btnEdit" Content="Изменить" Click="btnEdit_Click" 
                    Width="120" Margin="5" IsEnabled="False"/>
            <Button x:Name="btnDelete" Content="Удалить" Click="btnDelete_Click" 
                    Width="120" Margin="5" IsEnabled="False"/>
        </StackPanel>

        <!-- Список заказов -->
        <ListView x:Name="lvOrders" Grid.Row="1" Margin="5" SelectionChanged="lvOrders_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding OrderID}" Width="50"/>
                    <GridViewColumn Header="Дата" DisplayMemberBinding="{Binding OrderDate, StringFormat='{}{0:dd.MM.yyyy}'}" Width="100"/>
                    <GridViewColumn Header="Статус" DisplayMemberBinding="{Binding Status}" Width="100"/>
                    <GridViewColumn Header="Сумма" DisplayMemberBinding="{Binding TotalAmount, StringFormat=C}" Width="100"/>
                    <GridViewColumn Header="Оплата" DisplayMemberBinding="{Binding PaymentMethod}" Width="120"/>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Статус бар -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock x:Name="tbStatus"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Page>
